#pragma once

// ${generated_comment}

namespace at {

struct ${Dispatcher} final : public LegacyTHDispatcher {
  explicit ${Dispatcher}();

  ${dispatcher_derived_method_declarations}
};

} // namespace at
